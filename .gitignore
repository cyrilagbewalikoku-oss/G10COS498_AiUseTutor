# Local-only personal log (not pushed)
CHANGELOG.md

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.env

# OS / editor
.DS_Store
.vscode/
.idea/

# Build / dist
build/
dist/
*.egg-info/

# Per-learner session transcripts (runtime data, written by sage/session_store.py).
# The directory itself is kept via .gitkeep so the layout is visible.
data/interactions/*/

# Evaluation harness runtime data
evaluation/fixtures/simulated/*.json
evaluation/fixtures/exports/*.md
evaluation/fixtures/exports/*.txt
evaluation/results/*.json
evaluation/results/*.md
evaluation/results/.judge-cache.json
!evaluation/results/.gitkeep
!evaluation/fixtures/simulated/.gitkeep
!evaluation/fixtures/exports/.gitkeep
